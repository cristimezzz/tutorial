---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "关于";

const identity = {
    firstName: "Cristime",
    country: "China",
    occupation: "前端开发工程师",
    hobbies: ["编程", "阅读", "旅行"],
};

const skills = ["JavaScript", "TypeScript", "React", "Astro", "Node.js"];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle={pageTitle}>
    <style is:global define:vars={{ skillColor, fontWeight, textCase }}>
        h1 {
            color: purple;
            font-size: 4rem;
        }
        .skill {
            color: var(--skillColor);
            font-weight: var(--fontWeight);
            text-transform: var(--textCase);
        }
    </style>

    <p>我是一个前端开发工程师，我喜欢用代码来解决问题。</p>

    <p>以下是关于我的几个事实：</p>
    <ul>
        <li>我的名字是{identity.firstName}.</li>
        <li>我住在{identity.country}。我的职业是{identity.occupation}。</li>
        {
            identity.hobbies.length >= 2 && (
                <li>
                    我的两个习惯：{identity.hobbies[0]}和{identity.hobbies[1]}
                </li>
            )
        }
    </ul>
    <p>我的技能是：</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>

    {happy && <p>我很开心！</p>}
    {finished && <p>我完成了我的目标！</p>}
    {goal === 3 ? <p>我的目标是三天内完成</p> : <p>我的目标不是三天内完成</p>}
</BaseLayout>
